# https://github.com/jenkinsci/plugin-installation-manager-tool
plugins:
  - artifactId: configuration-as-code
  - artifactId: configuration-as-code-groovy
  - artifactId: allure-jenkins-plugin
    # createJenkins jobs via Groovy scripts without UI
  - artifactId: job-dsl
  - artifactId: docker-workflow
  - artifactId: workflow-aggregator

    # -- User Interface plugins --
  - artifactId: pipeline-graph-view
  - artifactId: pipeline-stage-view
  - artifactId: dark-theme
  # adds timestamps to the console output of Jenkins jobs
  - artifactId: timestamper
  # simple test results from JUnit XML. JUnit tag in pipeline script
  - artifactId: junit